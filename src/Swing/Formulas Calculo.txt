Indicaciones:

Crear las clases de acuerdo al diagrama de clases suministraddo, 
para los metodos calcular volumen y superficie utilizar las formulas que se les entrega
a continuación, para los constructores de cada figura ingresar los datos de volumen y
superficie con el método que los calcula de cada clase.

Para la creación de las ventanas se les facilita cada clase en la carpeta: Principal, VentanaPrincipal,
VentanaCilindro, VentanaEsfera, VentanaPiramide.

Una ves completado todo el código probar la funcionalidad.

Cilindro

calcular volumen
volumen = Math.PI * altura * Math.pow(radio, 2.0)

Calcular superficie
áreaLadoA = 2.0 * Math.PI * radio * altura
áreaLadoB = 2.0 * Math.PI * Math.pow(radio, 2.0)
superficie = áreaLadoA + áreaLadoB


Esfera

calcular volumen
volumen = 1.333 * Math.PI * Math.pow(this.radio, 3.0)

Calcular superficie
superficie = 4.0 * Math.PI * Math.pow(this.radio, 2.0)

Piramide

calcular volumen
volumen = (Math.pow(base, 2.0) * altura) / 3.0)

Calcular superficie
áreaBase = Math.pow(base, 2.0);
áreaLado = 2.0 * base * apotema;
superficie = áreaBase + áreaLado;
